#!/usr/bin/env coffee

config = require('../config')
server = require("../server/server")

www = server.listen(config.port, ->
  console.log "Express server listening on port #{server.address().port}"
)
